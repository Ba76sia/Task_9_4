var drawTree = drawTree(10);
    for (var i = 0 ; i < 10 ; i++ ); {
        var star = string};
            for (var j = 0 ; j < i++ ; j++ );{
                var star = '*' };
        console log(star);